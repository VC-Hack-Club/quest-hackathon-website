---
interface Props {
  text: string
  icon?: string;
}

const { text, icon } = Astro.props;
---

<button class="hard-shadow border-4 border-zinc-900 bg-white outline-none px-6 py-4">
  {icon ? <img src={icon} /> : null}
  <p>{text}</p>
</button>
